export enum PublicRoutes {
  LOGIN = "/login",
  REGISTER = "/register",
  LANDING = "/landing",
}

export enum PrivateRoutes {
  PRIVATE = "/private",
  DASHBOARD = "/dashboard",
  HOME = "/home",
  USERS = "/users",
  PHOTOGRAPHERS = "/photographers",
  ORGANIZERS = "/organizers",
  CLIENTS = "/clients",
  EVENTS = "/events",
  PHOTOS = "/photos",
  PHOTOSSALE = "/photos-sale",
  CART = "/cart",
  MYPHOTOS = "/my-photos",
  FACTURES = "/facturas",
  INVITATION = "/invitation",
}

export const baseUrl = "http://192.168.113.248:3000/";